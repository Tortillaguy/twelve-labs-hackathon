# Task 2: Navigation Demo Persistence
**Status**: PENDING
**Impact**: Medium (UX Flow)

The logo link currently resets the demo mode state.

### Requirements:
- [ ] Update `Header.tsx` to accept a `demoMode` boolean for the logo link.
- [ ] Use `Link` from `react-router-dom` to preserve `?demo=true`.
